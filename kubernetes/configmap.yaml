apiVersion: v1
kind: ConfigMap
metadata:
  name: intrusion-config
  namespace: intrusion-detector

data:
  APP_NAME: "SecureGen AI Intrusion Detector"
  ENVIRONMENT: "production"
  MLFLOW_TRACKING_URI: "sqlite:////app/mlflow.db"
  LOG_LEVEL: "INFO"